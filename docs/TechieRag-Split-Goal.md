# Goal: library-side repository separation (REQ-FN-006, BRD-87)

Context. On 2026-09-24 the TechieDesk application was copied into its own repository at `/mnt/c/1MyCode/Sevak` and renamed **Sevak** there (Sevak `REQ-FN-054`, goal file `Sevak/docs/Sevak-Split-Goal.md`). This repository becomes the library only: `src/*`, `tests/TechieRag.Tests`, the library docs, the package workflows. Decisions behind this goal: `docs/TechieRag-Update-Brief.md` decisions 1 to 6 (Sevak is TechieDesk renamed; Sevak is the application showing the full capabilities of TechieRag; freemium, limited not gated; split first) and `docs/OldDocs/Sevak-Decision-Request.md` D1 (Sevak repository private until v0.1). The owner commits; never run git writes.

**Precondition, check first.** Sevak's split goal must have finished green: `Sevak/.tfcore/.session/goal-done.json` says `complete`, and Sevak's checklist row `REQ-FN-054` is Implemented or Verified. If not, do items 4 to 9 only (documents and config), leave items 1 to 3 (deletions) undone, record why in the `REQ-FN-006` Remark, and finish as `blocked` naming the Sevak run as what must complete first.

Acceptance, all items:

1. **Delete the application source and its tests.** `apps/` (all five projects), `tests/TechieDesk.Tests`, `tests/appium`, `tests/verify`, `playwright.config.ts`, `uiIssues/`. Nothing under `src/` or `tests/TechieRag.Tests` changes.
2. **Delete the application documents.** `docs/TechieDesk-*.md` and `.html`, `docs/devguides/`, `docs/mockups/`, `docs/screenshots/TechieDesk/`, `docs/TrBlazeUI-AI-Reference.md`, `docs/trblazeui-issues-report.md`, `docs/TechieRag-TrBlazeUI-Feedback.md`, `.trblazeui/`. Keep `docs/screenshots/TechieRag/`. Keep every `*-TechieRag-Feedback.md` file (Chatur, MyDiary, TechieRag): those are feedback to the library.
3. **Delete the application workflow.** `.github/workflows/publish-desktop.yml`. Keep `.github/workflows/scripts/determine-version.sh` (used by `publish-nuget.yml`). Keep `publish-nuget.yml` and `publish-github-packages.yml` unchanged.
4. **`TechieRag.slnx`** lists `src/*`, `tests/TechieRag.Tests` and the library docs only; the `/apps/` folder and every app doc entry are removed. It builds with 0 errors through `bash .tfcore/utils/tf-build.sh` and `TechieRag.Tests` is green with `apps/` gone.
5. **`.tfcore/core-config.yaml`**: `metrics.project_type: library`; `devLoadAlwaysFiles` and `customTechnicalDocuments` name only `docs/TechieRag-*` files; the `runtimeVerification` comment block stays commented out (the library has no screens).
6. **Documents.** Run `*amend-docs TechieRag` with this change: (a) F-REPO (BRD-87): the separation happened on 2026-09-24; the application repository is Sevak, private until v0.1; the diagram's "TechieDesk repository" node becomes "Sevak repository". (b) F-WEB: retitled "Sevak application (moved)"; its body becomes a pointer paragraph: Sevak is the application showing the full capabilities of TechieRag, freemium with limited rather than gated features, ledgered in the Sevak repository; every BRD-N under F-WEB keeps its number and is marked `Moved to Sevak 2026-09-24`; rows already marked "Moved 2026-09-03" keep that mark. (c) The cross-repository reference rule: a requirement in another repository is written with the repository name, `Sevak#REQ-FN-054`, `TechieRag#REQ-RAG-054`; written into the BRD conventions section, `AGENTS.md` and `CLAUDE.md`. (d) Every remaining mention of TechieDesk in `README.md`, `docs/TechieRag-Architecture.md`, `-UsageGuide.md`, `-Coding-Standards.md`, `-DevGuide.md`, `-ProductGuide.md`, `-CompetitorAnalysis.md`, `docs/SETUP-AND-TESTING-GUIDE.md` and `.techierag/TechieRag-AI-Reference.md` is reviewed: where it names the application, it becomes Sevak with a link to the Sevak repository; where it is history (a dated decision, a feedback entry, a miss record), it stays as written. The competitor analysis line "mobile app: do not build" gains the clarification from plan 08: that decision was about a mobile application, not about the library running inside mobile applications. (e) `docs/TechieRag-Checklist.md`: REQ-UI-* and other application rows are set to `Moved to Sevak 2026-09-24` with the Sevak repository named, kept as pointers, never deleted; `REQ-FN-006` acceptance updated to this goal. (f) `PROJECT-STATUS.md` and `DECISIONS.md` keep library entries only, with one cross-link line to the Sevak repository; `DECISIONS.md` gains one entry: TechieDesk renamed Sevak and moved to its own repository on 2026-09-24, private until v0.1, this repository is the library from that date.
7. **Feedback replies.** Nothing in this goal closes a feedback entry. Do not write replies into `docs/Chatur-TechieRag-Feedback.md` or `docs/MyDiary-TechieRag-Feedback.md`; those come with the streaming, sign-in and local-model amendments later.
8. **`.gitignore`**: remove entries that only existed for the application (for example `apps/TechieDesk/appsettings.Development.json`); keep everything else.
9. **Verify.** `*verify functional` on `REQ-FN-006`; then the status gate, `PROJECT-STATUS.md` and `.html` updated, run record emitted. The final report lists every path deleted and every document changed, so the owner can review before committing.
